# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | data | Persistence strategy | IndexedDB via idb wrapper with debounced auto-save using Keystone onSnapshot | Local-first, no server dependency. User never loses work. | Yes — if sync is added in future | human |
| D002 | M001 | arch | State management library | MobX Keystone | Provides snapshots, undo middleware, model actions, and tree structure out of the box. Already in deps. | No | human |
| D003 | M001/S01 | data | Tree data structure: nested children array vs flat ObjectMap with parentId/sortOrder | Flat ObjectMap<OutlineItem> with parentId and sortOrder fields | Eliminates Keystone's single-parent detach() problem entirely (tree ops become property mutations). Produces sync-friendly ID-keyed patches instead of fragile array-index patches. Proven in spike: 15 tests passing for CRUD, indent/outdent, archive+undo, cursor restore. Trade-off: getChildren/getVisibleItems are O(n) scans, but fine for hundreds of items — can add caching later if needed. | Yes — could switch to nested if sync requirements change the calculus | agent |
| D004 | M001/S01 | data | Archive behavior for items with children | Cascade isArchived=true to all descendants when archiving an item | With the flat ObjectMap model, children of an archived parent would still appear in getChildren queries unless explicitly archived. Cascading ensures consistent visibility — archiving a parent hides its entire subtree. Undo reverses the cascade, restoring all descendants. Simpler than walking the parent chain in every query method. | Yes | agent |
| D005 | M001/S01/T02 | architecture | How outdentItem handles subsequent siblings of the outdented item | Re-parent subsequent siblings as children of the outdented item (standard outliner behavior) | This matches the behavior of WorkFlowy, Dynalist, and other established outliners. When outdenting a middle child, the items after it under the same parent logically belong to the outdented item's scope. The alternative (leaving them under the old parent) would break users' spatial mental model of the outline structure. | Yes | agent |
| D006 | M001/S01/T03 | library | Sort order storage strategy for OutlineItem ordering | Use fraci (fraciString with BASE62) for fractional indexing instead of sequential integers | Fractional indexing eliminates the need for renumberChildren after indent/outdent operations, generates keys between any two existing keys without touching other items, and is collaboration-ready. User requested it directly. The sortOrder field changed from number to string with lexicographic comparison. | Yes | human |
| D007 | M001/S01 | requirement | R001 — CRUD operations on tree structure | validated | 57 passing tests prove create (insertBelow/insertAbove), read (getItem/getChildren/getVisibleItems), update (setContent), and delete/archive (archiveItem with cascade) all work correctly including undo/redo. | Yes | agent |
| D008 | M001/S01 | requirement | R013 — soft-delete via archive instead of permanent destroy | validated | archiveItem tests prove isArchived is set (not deleted), cascade to descendants works, archived items are hidden from getVisibleItems/getChildren, and undo restores all archived descendants. Integration test exercises archive+undo in realistic workflow. | Yes | agent |
| D009 | M001/S02 | requirement | R004 — Inline editing with Enter/Esc commit flow | validated | 7 tests prove: Enter starts edit mode, Escape commits+stops editing, Enter in edit mode commits+stops+inserts new item below in edit mode, blur commits+stops editing (with double-commit guard), auto-focus on edit start, and content persistence through setContent API. All commit paths verified. | Yes | agent |
| D010 | M001/S02 | requirement | R003 — Keyboard-driven navigation and manipulation | validated | 14 keyboard handler tests prove all 16 navigation-mode shortcuts: j/k cursor movement, o/O insert+edit, Tab/Shift+Tab indent/outdent, Space complete, h/l collapse, Backspace archive-when-empty, Enter edit mode, Ctrl+Z/Shift+Z undo/redo, Ctrl+Arrow reorder. Edit-mode passthrough verified — shortcuts don't fire when editing. | Yes | agent |
| D011 | M001/S03 | requirement | R010 — Top bar with logo, search placeholder, nav placeholders | validated | Topbar.tsx delivers: Space Grotesk amber logo, disabled search input with / kbd hint, disabled Due/Tags/Lists nav links, help button (? icon), avatar placeholder circle. All non-functional placeholders as spec requires for M001. Fixed top with z-20. 81 tests pass. | Yes | agent |
| D012 | M001/S04 | architecture | Snapshot hydration strategy for IndexedDB load | fromSnapshot<AppState> to create a fresh tree, not applySnapshot on existing | fromSnapshot creates a clean new Keystone tree that supports registerRootStore, while applySnapshot would mutate an existing tree and can conflict with undo middleware setup. The fresh tree approach keeps bootstrap clean — load snapshot, register root, setup undo, clear undo history, wire auto-save listener. | Yes | agent |
